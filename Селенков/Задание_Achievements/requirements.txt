
# Основные зависимости
tkinter>=8.6  # обычно входит в стандартную поставку Python
sqlite3>=3.35  # обычно входит в стандартную поставку Python

# Внешние библиотеки
python-docx>=0.8.11
pandas>=1.5.0
openpyxl>=3.0.10
matplotlib>=3.6.0
tkcalendar>=1.6.1
schedule>=1.1.0
numpy>=1.23.0
pillow>=9.2.0  # для работы с изображениями

# Для тестирования (опционально)
pytest>=7.2.0
pytest-qt>=4.2.0
coverage>=6.5.0

# Дополнительные зависимости
faker>=15.0.0  # для генерации тестовых данных

# Зависимости для конкретных систем
# Windows: дополнительные зависимости не требуются
# Linux: может потребоваться установка tk через пакетный менеджер
# macOS: может потребоваться установка tk через brew

# Версия Python
python>=3.8

# Примечание: 
# 1. Некоторые модули (hashlib, re, json, os, datetime, threading, time, collections, shutil)
#    входят в стандартную библиотеку Python и не требуют отдельной установки
# 2. Для работы приложения необходимо установить все перечисленные выше пакеты
